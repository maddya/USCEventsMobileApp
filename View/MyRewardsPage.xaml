<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="USCEvents.MyRewardsPage" Title="My Rewards">
	<ContentPage.Content>
        <StackLayout>
            <Label Text="Available to Redeem" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" HeightRequest="40"/>
          
             <ListView x:Name="MyAvailable" RowHeight="96" SeparatorVisibility="None" ItemSelected="OnMyRewardSelected">
              <ListView.ItemTemplate>
                <DataTemplate>
                        <ImageCell ImageSource="{Binding RewardsImage}" Text="{Binding Title}" Detail="{Binding Points}"/>
                    </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            <Label Text="Redeemed" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" HeightRequest="40"/>
         

             <ListView x:Name="MyRedeemed" RowHeight="96" SeparatorVisibility="None" ItemSelected="OnMyRewardSelected">-->
             <ListView.ItemTemplate>
                <DataTemplate>
                        <ImageCell ImageSource="{Binding RewardsImage}" Text="{Binding Title}" Detail="{Binding Points}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
